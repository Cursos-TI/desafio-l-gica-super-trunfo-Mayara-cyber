# Desafio Super Trunfo: Comparação de Cartas de Países

Este projeto é uma implementação em C de uma parte do jogo "Super Trunfo", focado na comparação de atributos entre duas cartas de países. O desafio utiliza as estruturas `switch` para criar um menu interativo e a lógica de decisão aninhada (`if-else` dentro de `if-else`) para aplicar regras de comparação complexas.

## ⚙️ Tecnologias e Requisitos

* **Linguagem:** C
* **Compilador:** GCC (ou qualquer compilador C padrão)

---

## 🚀 Como Compilar e Executar o Programa

Siga os passos abaixo para baixar, compilar e executar o jogo no seu terminal.

### 1. Compilação

Para compilar o código-fonte (`super_trunfo.c`), use o comando `gcc`. O comando abaixo cria um arquivo executável chamado `jogo`:

```bash
gcc super_trunfo.c -o jogo